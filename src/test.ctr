in std = #import("std");

in fract = function (in a: uint64) {
        match (a) {
                case 0 : 1,
                case _ : multiply(a, fract ( sub( a, 1 ) ) )
        }
};

in main = function () {
        fract(.make(9))
};

main